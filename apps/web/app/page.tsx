export default function DashboardPage() {
  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="text-3xl font-semibold">Runback</h1>
      <p className="mt-2 text-zinc-600">
        Checkpointing and replay for Claude Code workflows.
      </p>
      <p className="mt-8 text-sm text-zinc-500">
        Plan 1 foundation is in place. The dashboard arrives in the frontend plan.
      </p>
    </main>
  );
}
